The following should be done to execute the code:
1. Call the buildTree function and pass the input values into it. e.g buildTree(_input).
2. Once this is done, the function will return sorted arrays of objects with their nodes and children
